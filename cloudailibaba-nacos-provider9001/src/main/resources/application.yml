server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848     # nacos 服务端的地址

management:
  endpoints:
    web:
      exposure:
        include: '*'






